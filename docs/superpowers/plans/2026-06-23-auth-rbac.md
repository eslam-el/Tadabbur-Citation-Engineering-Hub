# خطة تنفيذ: المصادقة ونظام الأدوار والصلاحيات (Auth + RBAC)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** إضافة مصادقة Google SSO ببوابة موافقة (PENDING→ACTIVE) ونظام صلاحيات (USER/ADMIN) يُفرض في ثلاث طبقات، مع أداة حذف آمنة للبيانات التجريبية.

**Architecture:** Auth.js v5 بإستراتيجية JWT. تقسيم الإعداد إلى `auth.config.ts` (آمن لبيئة Edge، بلا Prisma — يستخدمه middleware) و`auth.ts` (يحوي رد نداء `signIn` الذي ينفّذ upsert في Prisma — يعمل في بيئة Node عبر معالج المسار). الفرض في ثلاث طبقات: `middleware.ts` للصفحات، حُرّاس خادميون (`requireActive`/`requireAdmin`) لكل واجهة API، وإخفاء الأزرار في الواجهة. دوال الصلاحية نقية ومعزولة في `src/lib/permissions.ts` لتُختبر وحدويًا.

**Tech Stack:** Next.js 16 (App Router) · React 19 · TypeScript 5 · Prisma 6 + PostgreSQL (Neon) · Auth.js v5 (`next-auth@5`) · Vitest (اختبار الوحدة) · Tailwind v4.

## Global Constraints

- البناء **يجب** أن يستخدم `--webpack` دائمًا (Turbopack ينهار على علامات RTL غير المرئية في اسم المجلد). أوامر `npm run build` و`npm run dev` مضبوطة على ذلك بالفعل — لا تُغيّرها.
- البناء المحلي يتطلب متغيّر بيئة: `DATABASE_URL="postgresql://u:p@localhost:5432/db?sslmode=require"` (أو رابط Neon الحقيقي) وإلا فشل `prisma generate`/الاتصال.
- لا تُضِف `.env` إلى git إطلاقًا (يحوي اعتماد Neon). الأسرار تُضبط في Vercel Environment Variables.
- الفصل المفهومي ثابت: `role` = مسمّى وظيفي نصّي (مراجع/محقق…) لا يتغيّر معناه؛ `accessLevel` = الصلاحية (`USER`|`ADMIN`)؛ `status` = بوابة الموافقة (`PENDING`|`ACTIVE`).
- المدير الجذري = العضو الذي بريده == `process.env.ADMIN_EMAIL` (مقارنة بحروف صغيرة). يُفرض دائمًا `ADMIN`+`ACTIVE`، ولا يُخفَّض ولا يُحذف.
- التمييز القاطع للبيانات التجريبية: `email = NULL` (الحسابات الحقيقية تملك بريد جوجل دائمًا)؛ علم `seeded` احتياط.
- النصوص واجهةً بالعربية، اتجاه RTL، وألوان عبر متغيّرات CSS الموجودة (`var(--accent-gold)` إلخ) — حافظ على نفس الطابع البصري.
- العمل على فرع مستقل `feat/auth-rbac` (لا على `main`).

**ملاحظة تحقّق:** يتعذّر اختبار ردود نداء Auth.js وعمليات Prisma وحدويًا دون محاكاة ثقيلة؛ لذا تتبع وثيقة التصميم: **الدوال النقية فقط تُختبر بـ TDD** (Task 1)، وبقية المهام تُتحقَّق بـ `npm run build` الناجح (0 أخطاء TypeScript) ثم قائمة التحقق اليدوية الحيّة في Task 14.

---

## بنية الملفات

**ملفات جديدة:**
| ملف | المسؤولية |
|---|---|
| `vitest.config.ts` | إعداد مشغّل اختبار الوحدة (بيئة node). |
| `src/lib/permissions.ts` | دوال صلاحية **نقية**: `isActive`, `isAdmin` (بلا شبكة/قاعدة بيانات). |
| `src/lib/permissions.test.ts` | اختبارات وحدة للدوال النقية. |
| `src/auth.config.ts` | إعداد Auth.js آمن لبيئة Edge (مزوّد Google، callbacks: jwt/session، pages). بلا Prisma. |
| `src/auth.ts` | نسخة Node كاملة: `signIn` (upsert في Prisma) + تصدير `handlers, auth, signIn, signOut`. |
| `src/types/next-auth.d.ts` | توسعة أنواع الجلسة/التوكن (`memberId, accessLevel, status`). |
| `src/app/api/auth/[...nextauth]/route.ts` | معالج مسار المصادقة. |
| `src/middleware.ts` | حماية الصفحات وتوجيه `login`/`pending`. |
| `src/lib/api-guard.ts` | حُرّاس خادميون: `getAuthed`, `requireActive`, `requireAdmin`. |
| `src/lib/member-ops.ts` | حذف آمن مرتّب للأعضاء: `safeDeleteMember`, `cleanupSeededData` (معاملة واحدة، يعالج قيود FK). |
| `src/app/login/page.tsx` | شاشة الدخول بحساب Google. |
| `src/app/pending/page.tsx` | شاشة انتظار الاعتماد + ضبط الاسم. |
| `src/app/api/members/[id]/approve/route.ts` | اعتماد عضو (`PENDING`→`ACTIVE`) — مدير فقط. |
| `src/app/api/members/[id]/access/route.ts` | ترقية/خفض `accessLevel` — مدير فقط. |
| `src/app/api/members/demo/route.ts` | `DELETE` = حذف البيانات التجريبية — مدير فقط. |
| `.env.example` | توثيق متغيّرات البيئة المطلوبة. |

**ملفات معدّلة:**
| ملف | التعديل |
|---|---|
| `package.json` | إضافة `next-auth@5` و`vitest`؛ سكربت `test`. |
| `prisma/schema.prisma` | حقول `Member`: email, accessLevel, status, image, seeded. |
| `src/app/layout.tsx` | لفّ `SessionProvider`. |
| `src/lib/member-context.tsx` | استمداد العضو الحالي من الجلسة (إزالة localStorage و`setCurrent`)؛ إضافة `isAdmin`. |
| `src/app/api/members/route.ts` | GET يتطلب ACTIVE؛ POST للمدير فقط. |
| `src/app/api/members/[id]/route.ts` | PATCH (مدير لأي عضو / المستخدم لملفه)؛ DELETE آمن للمدير. |
| `src/app/api/reports/route.ts` | GET يتطلب ACTIVE؛ POST يتطلب ACTIVE ويأخذ المؤلف من الجلسة. |
| `src/app/api/reports/[id]/route.ts` | GET/PATCH/DELETE تتطلب ACTIVE. |
| `src/app/api/reports/[id]/comments/route.ts` | POST يتطلب ACTIVE والمؤلف من الجلسة. |
| `src/app/api/reports/[id]/solution/route.ts` | POST يتطلب ACTIVE والمؤلف من الجلسة. |
| `src/app/api/stats/route.ts`, `activity/route.ts`, `export/route.ts` | GET تتطلب ACTIVE. |
| `src/app/api/seed/route.ts` | POST للمدير فقط؛ ضبط `seeded:true` على المزروعين. |
| `src/components/app-header.tsx` | اسم المستخدم + زر خروج (إزالة منتقي العضوية). |
| `src/components/members-manager.tsx` | قسم «بانتظار الموافقة» + اعتماد/ترقية/خفض + «حذف البيانات التجريبية» + إخفاء أزرار الإدارة عن USER + تعديل الملف الشخصي فقط لغير المدير. |
| `src/app/page.tsx` | إزالة تنبيه «اختر عضويتك»؛ نقل زر البذر للمدير فقط. |

---

## Task 1: مشغّل الاختبار ودوال الصلاحية النقية (TDD)

**Files:**
- Modify: `package.json` (devDependencies + script)
- Create: `vitest.config.ts`
- Create: `src/lib/permissions.ts`
- Test: `src/lib/permissions.test.ts`

**Interfaces:**
- Produces:
  - `type AuthInfo = { memberId?: string | null; accessLevel?: string | null; status?: string | null } | null | undefined`
  - `isActive(u: AuthInfo): boolean` — `true` فقط إذا `status === "ACTIVE"`.
  - `isAdmin(u: AuthInfo): boolean` — `true` فقط إذا `status === "ACTIVE"` و`accessLevel === "ADMIN"`.

- [ ] **Step 1: تثبيت Vitest**

```bash
npm install -D vitest@^2
```

- [ ] **Step 2: إنشاء إعداد Vitest**

أنشئ `vitest.config.ts`:

```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    environment: "node",
    include: ["src/**/*.test.ts"],
  },
});
```

- [ ] **Step 3: إضافة سكربت الاختبار**

في `package.json` ضمن `"scripts"`، أضِف بعد سطر `"lint"`:

```json
    "test": "vitest run",
```

- [ ] **Step 4: كتابة الاختبار الفاشل**

أنشئ `src/lib/permissions.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { isActive, isAdmin } from "./permissions";

describe("isActive", () => {
  it("false لِـ null/undefined", () => {
    expect(isActive(null)).toBe(false);
    expect(isActive(undefined)).toBe(false);
  });
  it("false إذا الحالة PENDING", () => {
    expect(isActive({ status: "PENDING", accessLevel: "USER" })).toBe(false);
  });
  it("true إذا الحالة ACTIVE", () => {
    expect(isActive({ status: "ACTIVE", accessLevel: "USER" })).toBe(true);
  });
});

describe("isAdmin", () => {
  it("false لِـ null", () => {
    expect(isAdmin(null)).toBe(false);
  });
  it("false إذا ADMIN لكن PENDING", () => {
    expect(isAdmin({ status: "PENDING", accessLevel: "ADMIN" })).toBe(false);
  });
  it("false إذا ACTIVE لكن USER", () => {
    expect(isAdmin({ status: "ACTIVE", accessLevel: "USER" })).toBe(false);
  });
  it("true إذا ACTIVE و ADMIN", () => {
    expect(isAdmin({ status: "ACTIVE", accessLevel: "ADMIN" })).toBe(true);
  });
});
```

- [ ] **Step 5: تشغيل الاختبار للتأكد من فشله**

Run: `npm test`
Expected: FAIL — `Cannot find module './permissions'`.

- [ ] **Step 6: كتابة التنفيذ الأدنى**

أنشئ `src/lib/permissions.ts`:

```ts
// دوال صلاحية نقية — بلا شبكة أو قاعدة بيانات، قابلة للاختبار والاستخدام في بيئة Edge.
export type AuthInfo =
  | { memberId?: string | null; accessLevel?: string | null; status?: string | null }
  | null
  | undefined;

export function isActive(u: AuthInfo): boolean {
  return !!u && u.status === "ACTIVE";
}

export function isAdmin(u: AuthInfo): boolean {
  return !!u && u.status === "ACTIVE" && u.accessLevel === "ADMIN";
}
```

- [ ] **Step 7: تشغيل الاختبار للتأكد من نجاحه**

Run: `npm test`
Expected: PASS — جميع الاختبارات خضراء.

- [ ] **Step 8: Commit**

```bash
git add package.json package-lock.json vitest.config.ts src/lib/permissions.ts src/lib/permissions.test.ts
git commit -m "test: دوال الصلاحية النقية isActive/isAdmin مع Vitest"
```

---

## Task 2: هجرة مخطط Prisma — حقول المصادقة في Member

**Files:**
- Modify: `prisma/schema.prisma:14-27`

**Interfaces:**
- Produces: حقول جديدة على `Member`: `email String? @unique`, `accessLevel String @default("USER")`, `status String @default("PENDING")`, `image String?`, `seeded Boolean @default(false)`.

- [ ] **Step 1: إضافة الحقول إلى نموذج Member**

في `prisma/schema.prisma`، استبدل كتلة `model Member` (الأسطر 14-27) بحيث تُضاف الحقول الجديدة قبل العلاقات:

```prisma
model Member {
  id        String   @id @default(cuid())
  name      String
  role      String   @default("مراجع")
  color     String   @default("#c9a24b") // لون تمييزي لكل عضو
  initials  String   @default("")
  active    Boolean  @default(true)

  email       String?  @unique             // بريد جوجل — فريد. NULL = حساب تجريبي/قديم
  accessLevel String   @default("USER")     // "USER" | "ADMIN"
  status      String   @default("PENDING")  // "PENDING" | "ACTIVE"
  image       String?                       // صورة جوجل (اختياري)
  seeded      Boolean  @default(false)      // true لسجلات البذر

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  reports        ErrorReport[]     @relation("ReportAuthor")
  solvedReports  ErrorReport[]     @relation("ReportSolver")
  comments       ReportComment[]
}
```

- [ ] **Step 2: التحقق من صحة المخطط**

Run: `npx prisma validate`
Expected: `The schema at prisma\schema.prisma is valid 🚀`

- [ ] **Step 3: دفع المخطط إلى قاعدة البيانات وتوليد العميل**

> يتطلب `DATABASE_URL` صالحًا في `.env`. الحقول الجديدة كلها اختيارية أو لها قيمة افتراضية، فالدفع غير هدّام للبيانات القائمة (الأعضاء الحاليون يصبحون `email=NULL`, `accessLevel="USER"`, `status="PENDING"` — وهو المطلوب: يُعاملون كبيانات تجريبية قابلة للتنظيف).

Run: `npm run db:push`
Expected: `Your database is now in sync with your Prisma schema.` ثم `Generated Prisma Client`.

- [ ] **Step 4: Commit**

```bash
git add prisma/schema.prisma
git commit -m "feat(db): حقول المصادقة على Member (email/accessLevel/status/image/seeded)"
```

---

## Task 3: إعداد Auth.js v5 (config + node + معالج المسار + الأنواع)

**Files:**
- Modify: `package.json` (dependency)
- Create: `src/auth.config.ts`
- Create: `src/auth.ts`
- Create: `src/types/next-auth.d.ts`
- Create: `src/app/api/auth/[...nextauth]/route.ts`

**Interfaces:**
- Consumes: `isActive`/`AuthInfo` من `@/lib/permissions` (Task 1)؛ `db` من `@/lib/db`.
- Produces:
  - `authConfig` (default export من `@/auth.config`) — `NextAuthConfig` آمن لبيئة Edge.
  - من `@/auth`: `handlers`, `auth`, `signIn`, `signOut`. الجلسة تحمل على `session.user`: `memberId`, `accessLevel`, `status` (إضافةً إلى `name/email/image`).

- [ ] **Step 1: ترقية next-auth إلى v5**

```bash
npm install next-auth@beta
```

> `next-auth@beta` هو القناة الرسمية لإصدار v5. تأكّد أن `package.json` صار يشير إلى `^5.0.0-beta...`.

- [ ] **Step 2: توسعة الأنواع**

أنشئ `src/types/next-auth.d.ts`:

```ts
import type { DefaultSession } from "next-auth";

declare module "next-auth" {
  interface Session {
    user: {
      memberId?: string;
      accessLevel?: string;
      status?: string;
    } & DefaultSession["user"];
  }
}

declare module "next-auth/jwt" {
  interface JWT {
    memberId?: string;
    accessLevel?: string;
    status?: string;
  }
}
```

- [ ] **Step 3: الإعداد الآمن لبيئة Edge**

أنشئ `src/auth.config.ts`:

```ts
import type { NextAuthConfig } from "next-auth";
import Google from "next-auth/providers/google";

// إعداد آمن لبيئة Edge: لا يستورد Prisma. يستخدمه middleware والنسخة الكاملة في auth.ts.
export const authConfig = {
  providers: [
    Google({
      clientId: process.env.GOOGLE_CLIENT_ID,
      clientSecret: process.env.GOOGLE_CLIENT_SECRET,
    }),
  ],
  session: { strategy: "jwt" },
  pages: { signIn: "/login" },
  callbacks: {
    // يُنسخ الـ claims من user (الذي ملأه signIn في auth.ts) إلى التوكن مرة واحدة عند الدخول.
    jwt({ token, user }) {
      if (user) {
        const u = user as { memberId?: string; accessLevel?: string; status?: string };
        if (u.memberId) token.memberId = u.memberId;
        if (u.accessLevel) token.accessLevel = u.accessLevel;
        if (u.status) token.status = u.status;
      }
      return token;
    },
    session({ session, token }) {
      if (session.user) {
        session.user.memberId = token.memberId;
        session.user.accessLevel = token.accessLevel;
        session.user.status = token.status;
      }
      return session;
    },
  },
} satisfies NextAuthConfig;

export default authConfig;
```

- [ ] **Step 4: النسخة الكاملة (Node) مع upsert في Prisma**

أنشئ `src/auth.ts`:

```ts
import NextAuth from "next-auth";
import { authConfig } from "@/auth.config";
import { db } from "@/lib/db";

export const { handlers, auth, signIn, signOut } = NextAuth({
  ...authConfig,
  callbacks: {
    ...authConfig.callbacks,
    // يعمل في بيئة Node (معالج المسار) فقط — لا يُستدعى في middleware.
    async signIn({ user, profile }) {
      const email = (user?.email ?? profile?.email)?.toLowerCase();
      if (!email) return false;

      const name = user?.name ?? (profile?.name as string | undefined) ?? email;
      const image = user?.image ?? (profile?.picture as string | undefined) ?? null;
      const isRoot = !!process.env.ADMIN_EMAIL && email === process.env.ADMIN_EMAIL.toLowerCase();

      const existing = await db.member.findUnique({ where: { email } });
      let member;
      if (existing) {
        // المدير الجذري يُفرض دائمًا حتى لو عُدِّل سجله يدويًا.
        member = isRoot
          ? await db.member.update({
              where: { email },
              data: { accessLevel: "ADMIN", status: "ACTIVE", image },
            })
          : existing;
      } else {
        member = await db.member.create({
          data: {
            email,
            name,
            image,
            initials: name.slice(0, 2),
            accessLevel: isRoot ? "ADMIN" : "USER",
            status: isRoot ? "ACTIVE" : "PENDING",
            seeded: false,
          },
        });
      }

      // تمرير الـ claims إلى رد نداء jwt عبر كائن user.
      const u = user as { memberId?: string; accessLevel?: string; status?: string };
      u.memberId = member.id;
      u.accessLevel = member.accessLevel;
      u.status = member.status;
      return true;
    },
  },
});
```

- [ ] **Step 5: معالج المسار**

أنشئ `src/app/api/auth/[...nextauth]/route.ts`:

```ts
import { handlers } from "@/auth";

export const { GET, POST } = handlers;
```

- [ ] **Step 6: التحقق بالبناء**

Run: `npm run build`
Expected: البناء ينجح بلا أخطاء TypeScript. (تحذير محتمل عن `ADMIN_EMAIL`/`GOOGLE_*` غير مضبوطة وقت التشغيل لا يكسر البناء.)

- [ ] **Step 7: Commit**

```bash
git add package.json package-lock.json src/auth.config.ts src/auth.ts src/types/next-auth.d.ts "src/app/api/auth/[...nextauth]/route.ts"
git commit -m "feat(auth): إعداد Auth.js v5 (Google SSO، JWT، upsert للعضو عند الدخول)"
```

---

## Task 4: حماية الصفحات عبر middleware

**Files:**
- Create: `src/middleware.ts`

**Interfaces:**
- Consumes: `authConfig` من `@/auth.config`؛ `isActive` من `@/lib/permissions`.
- Produces: توجيه الصفحات — غير المصادَق → `/login`؛ المصادَق `PENDING` → `/pending`؛ `ACTIVE` → التطبيق. لا يشمل `/api` (يُفرض خادميًا عبر الحُرّاس).

- [ ] **Step 1: كتابة middleware**

أنشئ `src/middleware.ts`:

```ts
import NextAuth from "next-auth";
import { NextResponse } from "next/server";
import { authConfig } from "@/auth.config";
import { isActive } from "@/lib/permissions";

// نسخة Edge بلا Prisma — تفكّ التوكن فقط.
const { auth } = NextAuth(authConfig);

export default auth((req) => {
  const { pathname } = req.nextUrl;
  const authed = !!req.auth;
  const active = isActive(req.auth?.user);

  if (pathname === "/login") {
    return active ? NextResponse.redirect(new URL("/", req.url)) : NextResponse.next();
  }

  if (pathname === "/pending") {
    if (!authed) return NextResponse.redirect(new URL("/login", req.url));
    if (active) return NextResponse.redirect(new URL("/", req.url));
    return NextResponse.next();
  }

  if (!authed) return NextResponse.redirect(new URL("/login", req.url));
  if (!active) return NextResponse.redirect(new URL("/pending", req.url));
  return NextResponse.next();
});

// استثناء api والأصول الثابتة — يُفرض على الصفحات فقط.
export const config = {
  matcher: ["/((?!api|_next/static|_next/image|favicon.ico|.*\\.).*)"],
};
```

- [ ] **Step 2: التحقق بالبناء**

Run: `npm run build`
Expected: البناء ينجح ويظهر `middleware` في ملخّص المسارات (`ƒ Middleware`).

- [ ] **Step 3: Commit**

```bash
git add src/middleware.ts
git commit -m "feat(auth): حماية الصفحات وتوجيه login/pending عبر middleware"
```

---

## Task 5: الحُرّاس الخادميون وتطبيقهم على واجهات القراءة

**Files:**
- Create: `src/lib/api-guard.ts`
- Modify: `src/app/api/members/route.ts:5-20` (GET)
- Modify: `src/app/api/reports/route.ts:5-45` (GET)
- Modify: `src/app/api/reports/[id]/route.ts` (GET + PATCH + DELETE)
- Modify: `src/app/api/stats/route.ts:5`
- Modify: `src/app/api/activity/route.ts:5`
- Modify: `src/app/api/export/route.ts:7`

**Interfaces:**
- Consumes: `auth` من `@/auth`؛ `isActive`/`isAdmin` من `@/lib/permissions`.
- Produces (`@/lib/api-guard`):
  - `type Authed = { memberId: string; accessLevel: string; status: string; name?: string | null; email?: string | null }`
  - `getAuthed(): Promise<Authed | null>`
  - `requireActive(): Promise<{ ok: true; user: Authed } | { ok: false; res: NextResponse }>`
  - `requireAdmin(): Promise<{ ok: true; user: Authed } | { ok: false; res: NextResponse }>`

- [ ] **Step 1: كتابة الحُرّاس**

أنشئ `src/lib/api-guard.ts`:

```ts
import { NextResponse } from "next/server";
import { auth } from "@/auth";
import { isActive, isAdmin } from "@/lib/permissions";

export type Authed = {
  memberId: string;
  accessLevel: string;
  status: string;
  name?: string | null;
  email?: string | null;
};

export async function getAuthed(): Promise<Authed | null> {
  const session = await auth();
  const u = session?.user;
  if (!u?.memberId) return null;
  return {
    memberId: u.memberId,
    accessLevel: u.accessLevel ?? "USER",
    status: u.status ?? "PENDING",
    name: u.name,
    email: u.email,
  };
}

export async function requireActive() {
  const user = await getAuthed();
  if (!user) return { ok: false as const, res: NextResponse.json({ error: "unauthorized" }, { status: 401 }) };
  if (!isActive(user)) return { ok: false as const, res: NextResponse.json({ error: "forbidden" }, { status: 403 }) };
  return { ok: true as const, user };
}

export async function requireAdmin() {
  const user = await getAuthed();
  if (!user) return { ok: false as const, res: NextResponse.json({ error: "unauthorized" }, { status: 401 }) };
  if (!isAdmin(user)) return { ok: false as const, res: NextResponse.json({ error: "forbidden" }, { status: 403 }) };
  return { ok: true as const, user };
}
```

- [ ] **Step 2: حماية GET /api/members**

في `src/app/api/members/route.ts`، بعد سطري الاستيراد أضِف:

```ts
import { requireActive } from "@/lib/api-guard";
```

ثم في بداية دالة `GET` (أول سطر داخل `try`):

```ts
    const gate = await requireActive();
    if (!gate.ok) return gate.res;
```

- [ ] **Step 3: حماية GET /api/reports**

في `src/app/api/reports/route.ts` أضِف الاستيراد `import { requireActive } from "@/lib/api-guard";`، ثم أول سطر داخل `try` في `GET`:

```ts
    const gate = await requireActive();
    if (!gate.ok) return gate.res;
```

- [ ] **Step 4: حماية واجهات reports/[id] الثلاث**

في `src/app/api/reports/[id]/route.ts` أضِف الاستيراد `import { requireActive } from "@/lib/api-guard";`، ثم أضِف أول سطر داخل `try` في **كلٍّ** من `GET` و`PATCH` و`DELETE`:

```ts
    const gate = await requireActive();
    if (!gate.ok) return gate.res;
```

- [ ] **Step 5: حماية stats / activity / export**

في كل ملف من `src/app/api/stats/route.ts` و`src/app/api/activity/route.ts` و`src/app/api/export/route.ts`: أضِف `import { requireActive } from "@/lib/api-guard";`، ثم أول سطر داخل `try` في دالة `GET`:

```ts
    const gate = await requireActive();
    if (!gate.ok) return gate.res;
```

- [ ] **Step 6: التحقق بالبناء**

Run: `npm run build`
Expected: نجاح بلا أخطاء TypeScript.

- [ ] **Step 7: Commit**

```bash
git add src/lib/api-guard.ts src/app/api/members/route.ts src/app/api/reports src/app/api/stats/route.ts src/app/api/activity/route.ts src/app/api/export/route.ts
git commit -m "feat(authz): حُرّاس خادميون وفرض ACTIVE على واجهات القراءة"
```

---

## Task 6: ربط الهوية بالكتابة (المؤلف من الجلسة)

**Files:**
- Modify: `src/app/api/reports/route.ts:48-107` (POST)
- Modify: `src/app/api/reports/[id]/comments/route.ts` (POST)
- Modify: `src/app/api/reports/[id]/solution/route.ts` (POST)

**Interfaces:**
- Consumes: `requireActive` من `@/lib/api-guard` (Task 5).
- Produces: المؤلف (`authorId`/`solutionAuthorId`) يُؤخذ من `gate.user.memberId` ولا يُقبل من جسم الطلب (منع الانتحال).

- [ ] **Step 1: POST /api/reports يأخذ المؤلف من الجلسة**

في `src/app/api/reports/route.ts` داخل `POST`:
- أضِف أول سطر داخل `try`:

```ts
    const gate = await requireActive();
    if (!gate.ok) return gate.res;
```

- احذف السطر `const authorId = body?.authorId;` واستبدله بـ:

```ts
    const authorId = gate.user.memberId;
```

- في شرط التحقق، احذف `|| !authorId` (المؤلف مضمون الآن من الجلسة) ليصبح:

```ts
    if (!type || !title || !description) {
      return NextResponse.json(
        { error: "missing_fields", fields: { type, title, description } },
        { status: 400 }
      );
    }
```

> الاستيراد `requireActive` مضاف في Task 5 لهذا الملف بالفعل.

- [ ] **Step 2: POST تعليق يأخذ المؤلف من الجلسة**

في `src/app/api/reports/[id]/comments/route.ts`:
- أضِف الاستيراد: `import { requireActive } from "@/lib/api-guard";`
- أول سطر داخل `try`:

```ts
    const gate = await requireActive();
    if (!gate.ok) return gate.res;
```

- احذف `const authorId = body?.authorId;` واستبدله بـ `const authorId = gate.user.memberId;`
- عدّل شرط التحقق إلى `if (!bodyText) {` (المؤلف مضمون).

- [ ] **Step 3: POST حل يأخذ المؤلف من الجلسة**

في `src/app/api/reports/[id]/solution/route.ts`:
- أضِف الاستيراد: `import { requireActive } from "@/lib/api-guard";`
- أول سطر داخل `try`:

```ts
    const gate = await requireActive();
    if (!gate.ok) return gate.res;
```

- احذف `const solutionAuthorId = body?.solutionAuthorId;` واستبدله بـ `const solutionAuthorId = gate.user.memberId;`
- بما أن `solutionAuthorId` صار مضمونًا، بسّط كتلة الإسناد: استبدل `if (solutionAuthorId) { data.solutionAuthorId = solutionAuthorId; }` بـ `data.solutionAuthorId = solutionAuthorId;`

- [ ] **Step 4: التحقق بالبناء**

Run: `npm run build`
Expected: نجاح بلا أخطاء TypeScript.

- [ ] **Step 5: Commit**

```bash
git add src/app/api/reports/route.ts src/app/api/reports/[id]/comments/route.ts src/app/api/reports/[id]/solution/route.ts
git commit -m "feat(authz): اشتقاق مؤلف البلاغ/التعليق/الحل من الجلسة لا من الطلب"
```

---

## Task 7: الحذف الآمن للأعضاء + صلاحيات واجهات العضو

**Files:**
- Create: `src/lib/member-ops.ts`
- Modify: `src/app/api/members/route.ts:23-59` (POST)
- Modify: `src/app/api/members/[id]/route.ts` (PATCH + DELETE)

**Interfaces:**
- Consumes: `db` من `@/lib/db`؛ `getAuthed`/`requireAdmin` من `@/lib/api-guard`؛ `isAdmin` من `@/lib/permissions`.
- Produces (`@/lib/member-ops`):
  - `safeDeleteMember(id: string): Promise<{ members: number; reports: number }>`
  - `cleanupSeededData(): Promise<{ members: number; reports: number }>`

- [ ] **Step 1: وحدة الحذف الآمن المرتّب**

أنشئ `src/lib/member-ops.ts`:

```ts
import { db } from "@/lib/db";

// حذف آمن مرتّب لمجموعة أعضاء ضمن معاملة واحدة — يعالج قيود المفاتيح الأجنبية بالترتيب.
async function deleteMembers(ids: string[]): Promise<{ members: number; reports: number }> {
  if (ids.length === 0) return { members: 0, reports: 0 };
  return db.$transaction(async (tx) => {
    // (2) صفّر solutionAuthorId المُشير لمستهدَف في بلاغ مؤلفه ليس مستهدَفاً (حماية بيانات الآخرين)
    await tx.errorReport.updateMany({
      where: { solutionAuthorId: { in: ids }, authorId: { notIn: ids } },
      data: { solutionAuthorId: null },
    });
    // (3) احذف تعليقات المستهدفين على أي بلاغ (علاقة ReportComment.author إلزامية بلا onDelete)
    await tx.reportComment.deleteMany({ where: { authorId: { in: ids } } });
    // (4) احذف بلاغات المستهدفين (تعليقاتها المتبقية تُحذف تتاليّاً عبر onDelete: Cascade)
    const reports = await tx.errorReport.deleteMany({ where: { authorId: { in: ids } } });
    // (5) احذف سجلات نشاطهم
    await tx.activityLog.deleteMany({ where: { actorId: { in: ids } } });
    // (6) احذف الأعضاء
    const members = await tx.member.deleteMany({ where: { id: { in: ids } } });
    return { members: members.count, reports: reports.count };
  });
}

export async function safeDeleteMember(id: string) {
  return deleteMembers([id]);
}

export async function cleanupSeededData() {
  // (1) المستهدفون = الأعضاء بلا بريد جوجل (التجريبيون/القدامى)
  const targets = await db.member.findMany({ where: { email: null }, select: { id: true } });
  return deleteMembers(targets.map((t) => t.id));
}
```

- [ ] **Step 2: POST /api/members للمدير فقط**

في `src/app/api/members/route.ts`، أضِف الاستيراد `import { requireAdmin } from "@/lib/api-guard";` (إلى جانب `requireActive` الموجود)، ثم أول سطر داخل `try` في `POST`:

```ts
    const gate = await requireAdmin();
    if (!gate.ok) return gate.res;
```

- [ ] **Step 3: PATCH /api/members/[id] — المدير لأي عضو، المستخدم لملفه فقط**

استبدل دالة `PATCH` في `src/app/api/members/[id]/route.ts` بالكامل بـ:

```ts
export async function PATCH(
  req: NextRequest,
  { params }: { params: Promise<{ id: string }> }
) {
  try {
    const { id } = await params;
    const me = await getAuthed();
    if (!me) return NextResponse.json({ error: "unauthorized" }, { status: 401 });

    const admin = isAdmin(me);
    const isSelf = me.memberId === id;
    // المستخدم العادي لا يعدّل إلا ملفه الشخصي؛ غير المصادَق ACTIVE يُمنع إلا تعديل اسمه (لشاشة الانتظار).
    if (!admin && !isSelf) {
      return NextResponse.json({ error: "forbidden" }, { status: 403 });
    }

    const body = await req.json();
    const data: Record<string, unknown> = {};
    if (typeof body?.name === "string") data.name = body.name.trim();
    if (typeof body?.color === "string") data.color = body.color;
    if (typeof data.name === "string") data.initials = (data.name as string).slice(0, 2);
    // المسمّى الوظيفي والتفعيل: للمدير فقط.
    if (admin) {
      if (typeof body?.role === "string") data.role = body.role.trim();
      if (typeof body?.active === "boolean") data.active = body.active;
    }

    const member = await db.member.update({ where: { id }, data });
    return NextResponse.json({ member });
  } catch (e) {
    console.error("PATCH /api/members/[id]", e);
    return NextResponse.json({ error: "update_failed" }, { status: 500 });
  }
}
```

- [ ] **Step 4: DELETE /api/members/[id] — مدير + حذف آمن + حماية الجذري**

استبدل دالة `DELETE` في `src/app/api/members/[id]/route.ts` بالكامل بـ:

```ts
export async function DELETE(
  _req: NextRequest,
  { params }: { params: Promise<{ id: string }> }
) {
  try {
    const { id } = await params;
    const gate = await requireAdmin();
    if (!gate.ok) return gate.res;

    const target = await db.member.findUnique({ where: { id } });
    if (!target) return NextResponse.json({ error: "not_found" }, { status: 404 });

    // حماية المدير الجذري من الحذف.
    const root = process.env.ADMIN_EMAIL?.toLowerCase();
    if (target.email && root && target.email.toLowerCase() === root) {
      return NextResponse.json({ error: "cannot_delete_root" }, { status: 403 });
    }

    const result = await safeDeleteMember(id);
    return NextResponse.json({ ok: true, ...result });
  } catch (e) {
    console.error("DELETE /api/members/[id]", e);
    return NextResponse.json({ error: "delete_failed" }, { status: 500 });
  }
}
```

- [ ] **Step 5: تحديث استيرادات members/[id]**

تأكّد أن أعلى `src/app/api/members/[id]/route.ts` يحوي:

```ts
import { NextRequest, NextResponse } from "next/server";
import { db } from "@/lib/db";
import { getAuthed, requireActive, requireAdmin } from "@/lib/api-guard";
import { isAdmin } from "@/lib/permissions";
import { safeDeleteMember } from "@/lib/member-ops";
```

> `requireActive` مستخدَم في GET (Task 5)؛ إن لم يكن لهذا الملف GET فاحذفه من القائمة. (هذا الملف يحوي PATCH وDELETE فقط — احذف `requireActive` من السطر أعلاه.)

- [ ] **Step 6: التحقق بالبناء**

Run: `npm run build`
Expected: نجاح بلا أخطاء TypeScript.

- [ ] **Step 7: Commit**

```bash
git add src/lib/member-ops.ts src/app/api/members/route.ts src/app/api/members/[id]/route.ts
git commit -m "feat(authz): حذف آمن للأعضاء وصلاحيات إنشاء/تعديل العضو"
```

---

## Task 8: واجهتا الاعتماد والترقية (مدير فقط)

**Files:**
- Create: `src/app/api/members/[id]/approve/route.ts`
- Create: `src/app/api/members/[id]/access/route.ts`

**Interfaces:**
- Consumes: `requireAdmin` من `@/lib/api-guard`؛ `db`.
- Produces:
  - `PATCH /api/members/[id]/approve` → يضبط `status="ACTIVE"`. يُرجِع `{ member }`.
  - `PATCH /api/members/[id]/access` بجسم `{ accessLevel: "USER" | "ADMIN" }` → يضبط الصلاحية. يحمي المدير الجذري. يُرجِع `{ member }`.

- [ ] **Step 1: واجهة الاعتماد**

أنشئ `src/app/api/members/[id]/approve/route.ts`:

```ts
import { NextResponse } from "next/server";
import { db } from "@/lib/db";
import { requireAdmin } from "@/lib/api-guard";

export async function PATCH(
  _req: Request,
  { params }: { params: Promise<{ id: string }> }
) {
  try {
    const { id } = await params;
    const gate = await requireAdmin();
    if (!gate.ok) return gate.res;

    const member = await db.member.update({ where: { id }, data: { status: "ACTIVE" } });

    await db.activityLog.create({
      data: {
        actorId: gate.user.memberId,
        actorName: gate.user.name ?? null,
        action: "member_approved",
        targetType: "member",
        targetId: id,
      },
    });

    return NextResponse.json({ member });
  } catch (e) {
    console.error("PATCH /api/members/[id]/approve", e);
    return NextResponse.json({ error: "approve_failed" }, { status: 500 });
  }
}
```

- [ ] **Step 2: واجهة الترقية/الخفض**

أنشئ `src/app/api/members/[id]/access/route.ts`:

```ts
import { NextResponse } from "next/server";
import { db } from "@/lib/db";
import { requireAdmin } from "@/lib/api-guard";

export async function PATCH(
  req: Request,
  { params }: { params: Promise<{ id: string }> }
) {
  try {
    const { id } = await params;
    const gate = await requireAdmin();
    if (!gate.ok) return gate.res;

    const body = await req.json();
    const accessLevel = body?.accessLevel;
    if (accessLevel !== "USER" && accessLevel !== "ADMIN") {
      return NextResponse.json({ error: "invalid_access_level" }, { status: 400 });
    }

    const target = await db.member.findUnique({ where: { id } });
    if (!target) return NextResponse.json({ error: "not_found" }, { status: 404 });

    // المدير الجذري لا يُخفَّض.
    const root = process.env.ADMIN_EMAIL?.toLowerCase();
    if (target.email && root && target.email.toLowerCase() === root && accessLevel !== "ADMIN") {
      return NextResponse.json({ error: "cannot_demote_root" }, { status: 403 });
    }

    const member = await db.member.update({ where: { id }, data: { accessLevel } });

    await db.activityLog.create({
      data: {
        actorId: gate.user.memberId,
        actorName: gate.user.name ?? null,
        action: "member_access_changed",
        targetType: "member",
        targetId: id,
        meta: JSON.stringify({ accessLevel }),
      },
    });

    return NextResponse.json({ member });
  } catch (e) {
    console.error("PATCH /api/members/[id]/access", e);
    return NextResponse.json({ error: "access_change_failed" }, { status: 500 });
  }
}
```

- [ ] **Step 3: التحقق بالبناء**

Run: `npm run build`
Expected: نجاح؛ ظهور المسارين `/api/members/[id]/approve` و`/api/members/[id]/access`.

- [ ] **Step 4: Commit**

```bash
git add src/app/api/members/[id]/approve/route.ts src/app/api/members/[id]/access/route.ts
git commit -m "feat(admin): واجهتا اعتماد العضو وترقية/خفض الصلاحية"
```

---

## Task 9: حذف البيانات التجريبية + تأمين البذر

**Files:**
- Create: `src/app/api/members/demo/route.ts`
- Modify: `src/app/api/seed/route.ts:5-20`

**Interfaces:**
- Consumes: `requireAdmin` من `@/lib/api-guard`؛ `cleanupSeededData` من `@/lib/member-ops` (Task 7).
- Produces: `DELETE /api/members/demo` → يحذف الأعضاء بلا بريد وبياناتهم، يُرجِع `{ ok: true, members, reports }`.

- [ ] **Step 1: واجهة حذف البيانات التجريبية**

أنشئ `src/app/api/members/demo/route.ts`:

```ts
import { NextResponse } from "next/server";
import { requireAdmin } from "@/lib/api-guard";
import { cleanupSeededData } from "@/lib/member-ops";

export async function DELETE() {
  try {
    const gate = await requireAdmin();
    if (!gate.ok) return gate.res;

    const result = await cleanupSeededData();
    return NextResponse.json({ ok: true, ...result });
  } catch (e) {
    console.error("DELETE /api/members/demo", e);
    return NextResponse.json({ error: "cleanup_failed" }, { status: 500 });
  }
}
```

- [ ] **Step 2: تأمين البذر (مدير فقط + علم seeded)**

في `src/app/api/seed/route.ts`:
- أضِف الاستيراد: `import { requireAdmin } from "@/lib/api-guard";`
- أول سطر داخل `try`:

```ts
    const gate = await requireAdmin();
    if (!gate.ok) return gate.res;
```

- أضِف `seeded: true` إلى بيانات كل عضو من الثلاثة المزروعين. مثال للأول:

```ts
    const m1 = await db.member.create({
      data: { name: "أ. عبدالله", role: "محقق", color: "#c9a24b", initials: "ع", seeded: true },
    });
```

(كرّر بإضافة `, seeded: true` لـ `m2` و`m3`.)

- [ ] **Step 3: التحقق بالبناء**

Run: `npm run build`
Expected: نجاح؛ ظهور `/api/members/demo`.

- [ ] **Step 4: Commit**

```bash
git add src/app/api/members/demo/route.ts src/app/api/seed/route.ts
git commit -m "feat(admin): حذف البيانات التجريبية وتأمين البذر للمدير"
```

---

## Task 10: شاشتا الدخول والانتظار

**Files:**
- Create: `src/app/login/page.tsx`
- Create: `src/app/pending/page.tsx`

**Interfaces:**
- Consumes: `signIn`/`signOut`/`useSession` من `next-auth/react`.
- Produces: صفحتان عامتان (يسمح بهما middleware): `/login` و`/pending`.

- [ ] **Step 1: شاشة الدخول**

أنشئ `src/app/login/page.tsx`:

```tsx
"use client";

import { signIn } from "next-auth/react";
import { Button } from "@/components/ui/button";

export default function LoginPage() {
  return (
    <div className="min-h-screen flex items-center justify-center p-6">
      <div className="panel gold-rule max-w-md w-full p-8 text-center fade-up">
        <p className="text-xs tracking-widest font-semibold uppercase mb-2" style={{ color: "var(--accent-crimson)" }}>
          TADABBUR · CSL STUDIO
        </p>
        <h1 className="font-display text-3xl mb-3" style={{ color: "var(--accent-gold-bright)" }}>
          منصة تتبّع أخطاء الفريق
        </h1>
        <p className="text-sm mb-8" style={{ color: "var(--text-dim)" }}>
          سجّل الدخول بحساب Google للمتابعة. الحسابات الجديدة تنتظر اعتماد المدير قبل الوصول للبيانات.
        </p>
        <Button
          onClick={() => signIn("google", { callbackUrl: "/" })}
          className="w-full gap-2"
          style={{
            background: "linear-gradient(180deg, var(--accent-gold-bright), var(--accent-gold))",
            color: "var(--primary-foreground)",
          }}
        >
          الدخول بحساب Google
        </Button>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: شاشة الانتظار + ضبط الاسم**

أنشئ `src/app/pending/page.tsx`:

```tsx
"use client";

import { useState, useEffect } from "react";
import { signOut, useSession } from "next-auth/react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { toast } from "sonner";

export default function PendingPage() {
  const { data: session } = useSession();
  const memberId = session?.user?.memberId;
  const [name, setName] = useState("");
  const [saving, setSaving] = useState(false);

  useEffect(() => {
    if (session?.user?.name) setName(session.user.name);
  }, [session?.user?.name]);

  const saveName = async () => {
    if (!memberId || !name.trim()) return;
    setSaving(true);
    try {
      const res = await fetch(`/api/members/${memberId}`, {
        method: "PATCH",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ name: name.trim() }),
      });
      if (!res.ok) throw new Error();
      toast.success("تم حفظ اسمك. بانتظار اعتماد المدير.");
    } catch {
      toast.error("تعذّر حفظ الاسم.");
    } finally {
      setSaving(false);
    }
  };

  return (
    <div className="min-h-screen flex items-center justify-center p-6">
      <div className="panel gold-rule max-w-md w-full p-8 text-center fade-up">
        <h1 className="font-display text-2xl mb-3" style={{ color: "var(--accent-gold-bright)" }}>
          حسابك بانتظار الاعتماد
        </h1>
        <p className="text-sm mb-6" style={{ color: "var(--text-dim)" }}>
          تم تسجيلك بنجاح. لن ترى البيانات حتى يعتمد المدير حسابك. يمكنك ضبط اسمك الآن:
        </p>
        <div className="space-y-3">
          <Input
            value={name}
            onChange={(e) => setName(e.target.value)}
            placeholder="اسمك كما يظهر للفريق"
            className="bg-[var(--surface-2)] border-[var(--border-soft)] text-[var(--text-strong)]"
          />
          <Button
            onClick={saveName}
            disabled={saving}
            className="w-full"
            style={{
              background: "linear-gradient(180deg, var(--accent-gold-bright), var(--accent-gold))",
              color: "var(--primary-foreground)",
            }}
          >
            حفظ الاسم
          </Button>
          <Button
            variant="outline"
            onClick={() => signOut({ callbackUrl: "/login" })}
            className="w-full border-[var(--border-soft)] text-[var(--text-strong)]"
          >
            تسجيل الخروج
          </Button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: التحقق بالبناء**

Run: `npm run build`
Expected: نجاح؛ ظهور `/login` و`/pending`.

- [ ] **Step 4: Commit**

```bash
git add src/app/login/page.tsx src/app/pending/page.tsx
git commit -m "feat(auth): شاشتا الدخول وانتظار الاعتماد"
```

---

## Task 11: ربط الجلسة بالعميل (SessionProvider + member-context)

**Files:**
- Modify: `src/app/layout.tsx:1-67`
- Modify: `src/lib/member-context.tsx` (إعادة كتابة كاملة)

**Interfaces:**
- Consumes: `useSession`/`SessionProvider` من `next-auth/react`.
- Produces: سياق `useMember()` يُرجِع `{ current, members, loading, isAdmin, refresh }`. **أُزيل `setCurrent`.** `current` يُشتق من `session.user.memberId`.

- [ ] **Step 1: لفّ SessionProvider في layout**

في `src/app/layout.tsx`:
- أضِف بعد سطر استيراد `SonnerToaster`:

```ts
import { SessionProvider } from "next-auth/react";
```

- لفّ المحتوى داخل `ThemeProvider` بـ `SessionProvider`:

```tsx
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem={false}
          disableTransitionOnChange
        >
          <SessionProvider>
            {children}
            <Toaster />
            <SonnerToaster position="top-center" richColors />
          </SessionProvider>
        </ThemeProvider>
```

- [ ] **Step 2: إعادة كتابة member-context**

استبدل كامل `src/lib/member-context.tsx` بـ:

```tsx
"use client";

import { createContext, useContext, useEffect, useState, useCallback } from "react";
import { useSession } from "next-auth/react";

export type CurrentMember = {
  id: string;
  name: string;
  role: string;
  color: string;
  initials: string;
};

type Ctx = {
  current: CurrentMember | null;
  members: CurrentMember[];
  loading: boolean;
  isAdmin: boolean;
  refresh: () => Promise<void>;
};

const MemberCtx = createContext<Ctx>({
  current: null,
  members: [],
  loading: true,
  isAdmin: false,
  refresh: async () => {},
});

export function MemberProvider({ children }: { children: React.ReactNode }) {
  const { data: session, status } = useSession();
  const [members, setMembers] = useState<CurrentMember[]>([]);
  const [loading, setLoading] = useState(true);

  const memberId = session?.user?.memberId;
  const isAdmin = session?.user?.accessLevel === "ADMIN" && session?.user?.status === "ACTIVE";

  const refresh = useCallback(async () => {
    try {
      const res = await fetch("/api/members");
      if (!res.ok) {
        setMembers([]);
        return;
      }
      const data = await res.json();
      const list: CurrentMember[] = (data?.members || []).map((m: { id: string; name: string; role: string; color: string; initials?: string }) => ({
        id: m.id,
        name: m.name,
        role: m.role,
        color: m.color,
        initials: m.initials || m.name?.slice(0, 2) || "؟",
      }));
      setMembers(list);
    } catch (e) {
      console.error("MemberProvider refresh", e);
    } finally {
      setLoading(false);
    }
  }, []);

  useEffect(() => {
    if (status !== "loading") refresh();
  }, [status, refresh]);

  const current: CurrentMember | null =
    members.find((m) => m.id === memberId) ??
    (memberId
      ? {
          id: memberId,
          name: session?.user?.name ?? "",
          role: "",
          color: "#c9a24b",
          initials: (session?.user?.name ?? "؟").slice(0, 2),
        }
      : null);

  return (
    <MemberCtx.Provider value={{ current, members, loading, isAdmin, refresh }}>
      {children}
    </MemberCtx.Provider>
  );
}

export function useMember() {
  return useContext(MemberCtx);
}
```

- [ ] **Step 3: التحقق بالبناء**

Run: `npm run build`
Expected: قد يفشل بأخطاء TypeScript في `app-header.tsx`/`members-manager.tsx`/`page.tsx` بسبب `setCurrent` المحذوف — **هذا متوقّع** ويُعالَج في Task 12-14. تأكّد أن الأخطاء محصورة في تلك الملفات الثلاثة فقط (لا في `member-context.tsx` أو `layout.tsx`).

- [ ] **Step 4: Commit**

```bash
git add src/app/layout.tsx src/lib/member-context.tsx
git commit -m "feat(auth): اشتقاق العضو الحالي من الجلسة (SessionProvider) وإزالة localStorage"
```

---

## Task 12: ترويسة التطبيق — هوية المستخدم وتسجيل الخروج

**Files:**
- Modify: `src/components/app-header.tsx`

**Interfaces:**
- Consumes: `useMember()` (بلا `setCurrent`)؛ `signOut` من `next-auth/react`.
- Produces: ترويسة تعرض العضو الحالي وزر خروج؛ بلا منتقي عضوية. شارة عدد الأعضاء تظهر للمدير فقط.

- [ ] **Step 1: تحديث الاستيرادات وتفكيك السياق**

في `src/components/app-header.tsx`:
- استبدل `import { useState } from "react";` بـ — احذفه إن لم يَعُد `useState` مستخدمًا بعد إزالة الـ Sheet (سيُزال). أبقِ الباقي.
- أضِف: `import { signOut } from "next-auth/react";`
- أضِف أيقونة الخروج إلى استيراد lucide: `import { Plus, UserPlus, Users, LogOut } from "lucide-react";`
- استبدل `const { current, members, setCurrent } = useMember();` بـ `const { current, members, isAdmin } = useMember();`

- [ ] **Step 2: استبدال أدوات الترويسة (الكتلة من `{/* أدوات */}` حتى نهاية بطاقة العضو)**

استبدل كامل الكتلة `<div className="flex items-center gap-3 flex-wrap"> ... </div>` (الأسطر التي تبدأ بـ `{/* أدوات: تبديل الثيم + العضو الحالي */}` وتنتهي قبل `{/* شريط التبويبات */}`) بـ:

```tsx
        {/* أدوات: تبديل الثيم + هوية المستخدم */}
        <div className="flex items-center gap-3 flex-wrap">
          <ThemeToggle />

          {isAdmin && (
            <Button
              variant="outline"
              onClick={() => onTabChange("members")}
              className="gap-2 border-[var(--border-soft)] text-[var(--text-strong)] hover:bg-[var(--soft-gold-bg)]"
            >
              <Users className="w-4 h-4" />
              <span className="text-sm">الفريق</span>
              <span
                className="inline-flex items-center justify-center rounded-full text-xs font-bold"
                style={{
                  minWidth: 22,
                  height: 22,
                  padding: "0 6px",
                  background: "var(--soft-gold-bg)",
                  color: "var(--accent-gold-bright)",
                }}
              >
                {members.length}
              </span>
            </Button>
          )}

          {current && (
            <div className="flex items-center gap-2.5 px-3 py-1.5 rounded-lg" style={{ background: "var(--soft-gold-bg)", border: "1px solid var(--border-soft)" }}>
              <MemberAvatar name={current.name} color={current.color} initials={current.initials} size={32} />
              <div className="leading-tight">
                <p className="text-xs font-semibold text-[var(--text-strong)]">{current.name}</p>
                {current.role && <p className="text-xs text-[var(--text-dim)]">{current.role}</p>}
              </div>
            </div>
          )}

          <Button
            variant="ghost"
            size="icon"
            aria-label="تسجيل الخروج"
            title="تسجيل الخروج"
            onClick={() => signOut({ callbackUrl: "/login" })}
            className="text-[var(--text-dim)] hover:text-[var(--accent-crimson)] hover:bg-[var(--soft-crimson-bg)]"
          >
            <LogOut className="w-4 h-4" />
          </Button>
        </div>
```

- [ ] **Step 3: تنظيف الاستيرادات غير المستخدمة**

احذف استيرادات `Sheet, SheetContent, SheetDescription, SheetHeader, SheetTitle, SheetTrigger` و`useState` و`Plus`/`UserPlus` إن لم تَعُد مستخدمة. (الترويسة الجديدة تستخدم `Users` و`LogOut` فقط من lucide.) عدّل سطر lucide إلى: `import { Users, LogOut } from "lucide-react";`

> `onAddMember` لم يَعُد مستخدمًا في الترويسة. أبقِ توقيع الـ props كما هو حاليًا لتجنّب كسر `page.tsx` في هذه المهمة؛ سيُنظَّف في Task 14. (TypeScript لن يشكو من prop غير مستخدم.)

- [ ] **Step 4: التحقق بالبناء**

Run: `npm run build`
Expected: تختفي أخطاء `setCurrent` الخاصة بهذا الملف. قد تبقى أخطاء في `members-manager.tsx`/`page.tsx` (تُعالَج لاحقًا).

- [ ] **Step 5: Commit**

```bash
git add src/components/app-header.tsx
git commit -m "feat(ui): ترويسة بهوية المستخدم وزر خروج بدل منتقي العضوية"
```

---

## Task 13: لوحة إدارة الفريق (اعتماد/ترقية/خفض/حذف تجريبي)

**Files:**
- Modify: `src/components/members-manager.tsx`

**Interfaces:**
- Consumes: `useMember()` (`current`, `isAdmin`, `refresh`)؛ واجهات `approve`/`access`/`demo` (Task 8-9).
- Produces: لوحة إدارة كاملة. أزرار الإدارة (إضافة/حذف/اعتماد/ترقية/خفض/حذف تجريبي) تظهر للمدير فقط؛ المستخدم يرى القائمة ويعدّل ملفه فقط.

- [ ] **Step 1: تحديث النوع والاستيرادات وحالة الجلب**

في `src/components/members-manager.tsx`:
- أضِف الحقول إلى نوع `Member`:

```ts
type Member = {
  id: string;
  name: string;
  role: string;
  color: string;
  initials: string;
  active: boolean;
  email: string | null;
  accessLevel: string;
  status: string;
  createdAt: string;
  _count?: { reports: number; solvedReports: number };
};
```

- استبدل `const { current, setCurrent, refresh: refreshCtx } = useMember();` بـ `const { current, isAdmin, refresh: refreshCtx } = useMember();`
- أضِف أيقونات: عدّل استيراد lucide إلى:

```ts
import { UserPlus, Trash2, Pencil, Check, X, Users, Loader2, ShieldCheck, ShieldAlert, Clock, Sparkles } from "lucide-react";
```

- [ ] **Step 2: دوال الإجراءات الإدارية**

أضِف داخل مكوّن `MembersManager` (بعد دالة `onDelete`) هذه الدوال:

```tsx
  const approve = async (id: string) => {
    try {
      const res = await fetch(`/api/members/${id}/approve`, { method: "PATCH" });
      if (!res.ok) throw new Error();
      toast.success("تم اعتماد العضو.");
      refresh();
      refreshCtx();
    } catch {
      toast.error("تعذّر الاعتماد.");
    }
  };

  const setAccess = async (id: string, accessLevel: "USER" | "ADMIN") => {
    try {
      const res = await fetch(`/api/members/${id}/access`, {
        method: "PATCH",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ accessLevel }),
      });
      if (!res.ok) throw new Error();
      toast.success(accessLevel === "ADMIN" ? "تمت الترقية إلى مدير." : "تم الخفض إلى مستخدم.");
      refresh();
      refreshCtx();
    } catch {
      toast.error("تعذّر تغيير الصلاحية.");
    }
  };

  const deleteDemo = async () => {
    if (!confirm("سيتم حذف جميع الأعضاء التجريبيين (بلا بريد) وبلاغاتهم نهائيًا. متابعة؟")) return;
    try {
      const res = await fetch("/api/members/demo", { method: "DELETE" });
      const data = await res.json();
      if (!res.ok) throw new Error();
      toast.success(`تم حذف ${data.members} عضوًا تجريبيًا و${data.reports} بلاغًا.`);
      refresh();
      refreshCtx();
    } catch {
      toast.error("تعذّر حذف البيانات التجريبية.");
    }
  };
```

- [ ] **Step 3: تحديث `onDelete` (رسالة صادقة) وحارس الإضافة**

استبدل دالة `onDelete` بـ:

```tsx
  const onDelete = async (id: string) => {
    if (id === current?.id) {
      toast.error("لا يمكنك حذف حسابك أثناء استخدامه.");
      return;
    }
    if (!confirm("هل تريد حذف هذا العضو؟ ستُحذف بلاغاته وتعليقاته معه نهائيًا.")) return;
    try {
      const res = await fetch(`/api/members/${id}`, { method: "DELETE" });
      if (!res.ok) throw new Error();
      toast.success("تم حذف العضو.");
      refresh();
      refreshCtx();
    } catch {
      toast.error("تعذّر الحذف.");
    }
  };
```

- [ ] **Step 4: فصل القوائم وعرض قسم الانتظار وشريط أدوات المدير**

استبدل كتلة العرض (من `{loading ? (` حتى إغلاق الشبكة `)}` قبل `<AddMemberDialog`) بمنطق يفصل المعلّقين عن النشطين ويُظهر أدوات المدير. أولًا، احسب القوائم قبل `return` (بعد `useEffect`):

```tsx
  const pending = members.filter((m) => m.status === "PENDING");
  const activeMembers = members.filter((m) => m.status !== "PENDING");
  const hasDemo = members.some((m) => !m.email);
```

ثم في زر «إضافة عضو» بالترويسة العلوية للوحة، لُفّه بشرط المدير: `{isAdmin && ( ...الزرّ... )}`.

أضِف شريط أدوات المدير بعد كتلة الترويسة (`</div>` المغلقة للوحة العنوان) وقبل كتلة `{loading ? ...}`:

```tsx
      {isAdmin && hasDemo && (
        <div className="panel p-4 fade-up flex flex-wrap items-center justify-between gap-3">
          <div className="flex items-center gap-2 text-sm" style={{ color: "var(--text-dim)" }}>
            <Sparkles className="w-4 h-4" style={{ color: "var(--accent-gold-bright)" }} />
            يوجد أعضاء/بيانات تجريبية. يمكنك حذفها لتفادي الالتباس بالبيانات الحقيقية.
          </div>
          <Button
            onClick={deleteDemo}
            variant="outline"
            className="gap-2 border-[var(--accent-crimson)] text-[var(--accent-crimson)] hover:bg-[var(--soft-crimson-bg)]"
          >
            <Trash2 className="w-4 h-4" />
            حذف البيانات التجريبية
          </Button>
        </div>
      )}

      {isAdmin && pending.length > 0 && (
        <div className="panel p-4 fade-up">
          <div className="flex items-center gap-2 mb-3">
            <Clock className="w-4 h-4" style={{ color: "var(--accent-gold-bright)" }} />
            <h3 className="text-sm font-bold m-0" style={{ color: "var(--text-strong)" }}>
              بانتظار الموافقة ({pending.length})
            </h3>
          </div>
          <div className="space-y-2">
            {pending.map((m) => (
              <div key={m.id} className="flex items-center gap-3 p-2 rounded-lg" style={{ background: "var(--surface-2)", border: "1px solid var(--border-soft)" }}>
                <MemberAvatar name={m.name} color={m.color} initials={m.initials} size={36} />
                <div className="flex-1 min-w-0">
                  <p className="text-sm font-semibold m-0 truncate" style={{ color: "var(--text-strong)" }}>{m.name}</p>
                  <p className="text-xs m-0 truncate" style={{ color: "var(--text-dim)" }}>{m.email}</p>
                </div>
                <Button size="sm" onClick={() => approve(m.id)} className="gap-1.5" style={{ background: "var(--accent-green)", color: "var(--primary-foreground)" }}>
                  <Check className="w-3.5 h-3.5" /> اعتماد
                </Button>
                <Button size="sm" variant="ghost" onClick={() => onDelete(m.id)} className="text-[var(--accent-crimson)] hover:bg-[var(--soft-crimson-bg)]">
                  <X className="w-3.5 h-3.5" />
                </Button>
              </div>
            ))}
          </div>
        </div>
      )}
```

وفي شبكة الأعضاء، بدّل `{members.map(` إلى `{activeMembers.map(` ومرّر الخصائص الجديدة إلى `MemberCard`:

```tsx
              <MemberCard
                key={m.id}
                member={m}
                isCurrent={m.id === current?.id}
                isAdmin={isAdmin}
                isEditing={editingId === m.id}
                onEdit={() => setEditingId(m.id)}
                onStopEdit={() => setEditingId(null)}
                onDelete={() => onDelete(m.id)}
                onPromote={() => setAccess(m.id, "ADMIN")}
                onDemote={() => setAccess(m.id, "USER")}
                onUpdated={() => {
                  setEditingId(null);
                  refresh();
                  refreshCtx();
                }}
              />
```

> أزِل خاصية `onSetCurrent` تمامًا من الاستدعاء.

- [ ] **Step 5: تحديث `MemberCard` (أزرار المدير + تعديل الملف فقط)**

في توقيع `MemberCard`، استبدل `isCurrent, isEditing, onEdit, onStopEdit, onDelete, onSetCurrent, onUpdated` بـ:

```tsx
function MemberCard({
  member,
  isCurrent,
  isAdmin,
  isEditing,
  onEdit,
  onStopEdit,
  onDelete,
  onPromote,
  onDemote,
  onUpdated,
}: {
  member: Member;
  isCurrent: boolean;
  isAdmin: boolean;
  isEditing: boolean;
  onEdit: () => void;
  onStopEdit: () => void;
  onDelete: () => void;
  onPromote: () => void;
  onDemote: () => void;
  onUpdated: () => void;
}) {
```

أضِف شارة الصلاحية بجوار شارة «أنت» (داخل الكتلة العلوية للبطاقة، بعد كتلة `{isCurrent && (...)}`):

```tsx
        {member.accessLevel === "ADMIN" && (
          <span className="text-xs px-1.5 py-0.5 rounded inline-flex items-center gap-1" style={{ background: "var(--soft-gold-bg)", color: "var(--accent-gold-bright)" }}>
            <ShieldCheck className="w-3 h-3" /> مدير
          </span>
        )}
```

استبدل كتلة الأزرار غير-التحريرية (الفرع `: (` الذي يحوي «تعيين كعضوي» و«تعديل» و«حذف») بـ:

```tsx
          <>
            <Button
              size="sm"
              variant="outline"
              onClick={onEdit}
              className="gap-1.5 flex-1 border-[var(--border-soft)] text-[var(--text-strong)] hover:bg-[var(--soft-gold-bg)]"
            >
              <Pencil className="w-3.5 h-3.5" /> تعديل
            </Button>
            {isAdmin && !isCurrent && (
              <>
                {member.accessLevel === "ADMIN" ? (
                  <Button size="sm" variant="ghost" onClick={onDemote} title="خفض إلى مستخدم" className="text-[var(--text-dim)] hover:bg-[var(--soft-gold-bg)]">
                    <ShieldAlert className="w-3.5 h-3.5" />
                  </Button>
                ) : (
                  <Button size="sm" variant="ghost" onClick={onPromote} title="ترقية إلى مدير" className="text-[var(--text-dim)] hover:bg-[var(--soft-gold-bg)]">
                    <ShieldCheck className="w-3.5 h-3.5" />
                  </Button>
                )}
                <Button size="sm" variant="ghost" onClick={onDelete} className="text-[var(--accent-crimson)] hover:bg-[var(--soft-crimson-bg)]">
                  <Trash2 className="w-3.5 h-3.5" />
                </Button>
              </>
            )}
          </>
```

> زر «تعديل» متاح للجميع لكنه يعدّل ملف العضو نفسه فقط خادميًا (Task 7)؛ المستخدم العادي عمليًا يعدّل بطاقته. أزرار الترقية/الخفض/الحذف للمدير فقط وغير ظاهرة على بطاقته هو.

في دالة `save` داخل `MemberCard`، احذف إرسال `role` لغير المدير: أبقِ الإرسال كما هو (الخادم يتجاهل `role` لغير المدير تلقائيًا). لا تغيير لازم.

- [ ] **Step 6: التحقق بالبناء**

Run: `npm run build`
Expected: تختفي أخطاء `setCurrent`/`onSetCurrent` لهذا الملف.

- [ ] **Step 7: Commit**

```bash
git add src/components/members-manager.tsx
git commit -m "feat(admin): لوحة إدارة الفريق (اعتماد/ترقية/خفض/حذف تجريبي) بصلاحيات"
```

---

## Task 14: تنظيف الصفحة الرئيسة + التوثيق + التحقق النهائي

**Files:**
- Modify: `src/app/page.tsx`
- Create: `.env.example`

**Interfaces:**
- Consumes: `useMember()` (`isAdmin`)؛ لا منتقي عضوية.

- [ ] **Step 1: إزالة تنبيه «اختر عضويتك» وتقييد البذر بالمدير**

في `src/app/page.tsx`:
- استبدل `const { current, members, loading } = useMember();` بـ `const { isAdmin } = useMember();`
- احذف كتلة التنبيه بالكامل (`{/* تنبيه عند عدم اختيار عضو */}` وكامل شرط `!loading && !current && ...`).
- في تمرير props للترويسة، احذف `onAddMember={() => setTab("members")}` إن لم يَعُد مطلوبًا، **أو** أبقِه إن بقي توقيع `Header` يطلبه. (راجع Task 12: أبقينا التوقيع، فأبقِ التمرير لتفادي خطأ.)
- لُفّ كتلة زر البذر بشرط المدير: استبدل `{tab === "dashboard" && (` المحيطة بزر البذر بـ `{tab === "dashboard" && isAdmin && (`.

- [ ] **Step 2: ملف توثيق البيئة**

أنشئ `.env.example`:

```bash
# قاعدة البيانات (Neon)
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# المصادقة (Auth.js v5)
AUTH_SECRET="ولّده بـ: npx auth secret"
AUTH_URL="http://localhost:3000"   # في الإنتاج: https://<vercel-domain>

# Google OAuth (من Google Cloud Console)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# المدير الجذري (بريد جوجل — يُمنح ADMIN+ACTIVE دائمًا)
ADMIN_EMAIL="you@example.com"
```

- [ ] **Step 3: البناء النهائي**

Run: `npm run build`
Expected: نجاح تام، 0 أخطاء TypeScript، لا أخطاء lint كاسرة.

- [ ] **Step 4: تشغيل اختبارات الوحدة**

Run: `npm test`
Expected: PASS لجميع اختبارات `permissions`.

- [ ] **Step 5: Commit**

```bash
git add src/app/page.tsx .env.example
git commit -m "feat(ui): إزالة منتقي العضوية وتقييد البذر بالمدير + توثيق البيئة"
```

- [ ] **Step 6: قائمة التحقق اليدوية الحيّة**

> تتطلب ضبط متغيّرات البيئة وبيانات Google OAuth (محليًا برابط `http://localhost:3000/api/auth/callback/google`، وفي Vercel برابط الدومين). نفّذ بعد ضبطها:

1. زائر غير مسجَّل يفتح أي صفحة → يُحوَّل إلى `/login`.
2. دخول حساب جديد → يُنشأ `PENDING` → شاشة `/pending`، لا بيانات؛ يمكنه ضبط اسمه.
3. دخول `ADMIN_EMAIL` → `ADMIN`+`ACTIVE` مباشرة، يرى كل شيء.
4. المدير يعتمد العضو الجديد → يصبح `ACTIVE` ويرى البيانات بعد إعادة دخوله/تحديث جلسته.
5. المدير يرقّي عضوًا → تظهر له أزرار الإدارة؛ يخفضه → تختفي.
6. `USER` لا يرى أزرار الإدارة؛ نداء `PATCH /api/members/<other>/access` مباشرة → `403`.
7. نداء أي واجهة من حساب `PENDING` → `403`؛ والصفحات تُحوِّل إلى `/pending`.
8. «حذف البيانات التجريبية» يحذف الأعضاء بلا بريد وبلاغاتهم فقط، ويُبقي الحقيقية (تحقّق من بقاء تعليق تجريبي على بلاغ حقيقي محذوفًا والبلاغ الحقيقي سالمًا).
9. تسجيل الخروج يعيد إلى `/login`.
10. محاولة حذف/خفض المدير الجذري → `403`.

---

## مراجعة ذاتية (أُجريت)

**تغطية المواصفات:** كل بند في وثيقة التصميم له مهمة:
- الجداران (مصادقة/صلاحيات) → Tasks 3-9. بوابة PENDING → Tasks 3,4,8,10. المدير الجذري → Tasks 3,7,8. فصل `accessLevel` عن `role` → Tasks 2,7. الهوية=العضو → Tasks 6,11,12. أداة حذف التجريبية + المعاملة المرتّبة 6 خطوات → Task 7 (الوحدة) + Task 9 (الواجهة). إصلاح الحذف العام → Task 7. متغيّرات البيئة → Task 14. التحقق (وحدة+بناء+يدوي) → Tasks 1,14.

**مسح العناصر النائبة:** لا توجد عبارات TBD/TODO؛ كل خطوة تحوي الكود الفعلي أو الأمر مع المخرَج المتوقّع.

**اتساق الأنواع:** `Authed.memberId` (Task 5) يُستهلك في Tasks 6,7,8,9. `isActive/isAdmin` (Task 1) تُستهلك في Tasks 4,5. `safeDeleteMember`/`cleanupSeededData` (Task 7) تُستهلك في Tasks 7,9. توسعة `session.user.{memberId,accessLevel,status}` (Task 3) تُستهلك في Tasks 5,11. `useMember()` فقد `setCurrent` وكسبَ `isAdmin` (Task 11) — مُعالَج في كل مستهلكيه (Tasks 12,13,14).
